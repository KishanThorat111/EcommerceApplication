{
  "name": "ecommersapplication",
  "version": "1.0.0",
  "main": "./cookie-server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-server": "cd cookie-server && node server.js",
    "start-client": "cd cookie-client && npm start",
    "start": "concurrently \"npm run start-client\" \"npm run start-server\"",
    "build-client": "cd cookie-client && ng build",
    "build": "npm run build-client"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}
